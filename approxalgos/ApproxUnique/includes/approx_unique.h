#include <stdbool.h>

int 
approx_unique (
	       int * x,
	       char *cfld,
	       long long siz,
	       long long *y,
	       double *ptr_estimate_accuracy_percent,
	       int *ptr_estimate_is_good
		   );
